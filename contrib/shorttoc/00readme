The shorttoc package allows a second table of contents,
with a different depth than the main one.

Run latex on shorttoc.ins to get shorttoc.sty.
Run latex three times (yes, 3) on shorttoc.dtx to get the documentation.

Note that two TOCs are present in the documentation,
with different depths. 

